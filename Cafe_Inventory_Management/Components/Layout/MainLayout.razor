@inherits LayoutComponentBase
@inject IDialogService Dialog
@using Cafe_Inventory_Management.UI.Components.Pages
@using Cafe_Inventory_Management.UI.Services
@using MudBlazor
@inject NavigationManager Navigation
@inject AuthenticationStateProvider AuthenticationStateProvider

<MudThemeProvider IsDarkMode="@_isDarkMode" Theme="_theme" />
<MudPopoverProvider />
<MudDialogProvider FullWidth="true" CloseButton="true" MaxWidth="MaxWidth.ExtraSmall" />
<MudSnackbarProvider />
<style>
    /* Premium Scrollbar Styling */
    ::-webkit-scrollbar {
        width: 8px;
        height: 8px;
    }
    ::-webkit-scrollbar-track {
        background: transparent;
    }
    ::-webkit-scrollbar-thumb {
        background: rgba(0, 0, 0, 0.1);
        border-radius: 10px;
    }
    ::-webkit-scrollbar-thumb:hover {
        background: rgba(0, 0, 0, 0.2);
    }
    
    .dark-mode-scrollbar::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.1);
    }
</style>
@if (!IsLoginPage)
{
    <MudLayout>
        @if (!IsLoginPage)
        {
            <MudAppBar Elevation="1" Dense="true" Fixed="true" Class="appbar">

                <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2" Class="brand-stack">
                    <MudIcon Icon="@Icons.Material.Filled.LocalCafe" Color="Color.Primary" Size="Size.Medium" />
                    <MudText Typo="Typo.h6" FontWeight="FontWeight.Bold" Class="brand-title">
                        CHANOHOLIC
                    </MudText>
                </MudStack>


                <MudSpacer />

                <MudToggleIconButton @bind-Toggled="@_isDarkMode"
                                   Icon="@Icons.Material.Filled.DarkMode"
                                   ToggledIcon="@Icons.Material.Filled.LightMode"
                                   Title="Toggle Dark Mode"
                                   Class="mr-2"
                                   Color="Color.Inherit" />


                <AuthorizeView>
                    <Authorized Context="auth">
                        <StockAlertBanner />

                        <MudLink Href="/profile" Underline="Underline.None" Color="Color.Inherit" Class="mr-3 user-name d-flex align-center">
                            <MudAvatar Color="Color.Primary" Size="Size.Small" Class="mr-2">
                                @(auth.User.FindFirst(c => c.Type == "name")?.Value?.FirstOrDefault() ?? 'U')
                            </MudAvatar>
                            <MudText Typo="Typo.body1">
                                 <b>@auth.User.FindFirst(c => c.Type == "name")?.Value</b>
                            </MudText>
                        </MudLink>

                        <MudButton OnClick="Logout"
                                   Variant="Variant.Outlined"
                                   Color="Color.Dark"
                                   StartIcon="@Icons.Material.Filled.Logout">
                            Logout
                        </MudButton>


                    </Authorized>
                </AuthorizeView>

            </MudAppBar>


            <MudDrawer Open="true" ClipMode="DrawerClipMode.Always" Elevation="2" Class="drawer">
                <NavMenu />
            </MudDrawer>
            <MudMainContent Class="main-content">
                <div>
                    @Body
                </div>
            </MudMainContent>
        }
        else
        {
            <MudMainContent Style="background-color: #111827;">
                @Body
            </MudMainContent>
        }
    </MudLayout>
}
else
{
        <MudLayout>
        @Body

</MudLayout>
}

@code {
    private bool _isDarkMode = false;

    private readonly MudTheme _theme = new()
    {
        PaletteLight = new PaletteLight
        {
            Primary = "#4b2e2b",
            PrimaryContrastText = "#ffffff",
            Secondary = "#6b4f4f",
            Background = "#faf7f2",
            Surface = "#ffffff",
            DrawerBackground = "#ffffff",
            DrawerText = "#1b1212",
            AppbarBackground = "#ffffff",
            AppbarText = "#1b1212",
            Divider = "rgba(17, 24, 39, 0.10)",
            ActionDefault = "#1b1212"
        },
        PaletteDark = new PaletteDark
        {
            Primary = "#8d6e63",
            Secondary = "#bcaaa4",
            Background = "#121212",
            Surface = "#1e1e1e",
            DrawerBackground = "#1e1e1e",
            DrawerText = "rgba(255,255,255,0.8)",
            AppbarBackground = "#121212",
            AppbarText = "rgba(255,255,255,0.9)",
            Divider = "rgba(255, 255, 255, 0.12)",
            ActionDefault = "rgba(255,255,255,0.7)"
        },
        LayoutProperties = new LayoutProperties
        {
            DefaultBorderRadius = "12px",
        }
    };

    private bool IsLoginPage => Navigation.ToBaseRelativePath(Navigation.Uri) == "" ||
                                Navigation.ToBaseRelativePath(Navigation.Uri).ToLower().Contains("signin") ||
                                Navigation.ToBaseRelativePath(Navigation.Uri).ToLower().Contains("login");

    private async Task Logout()
    {
        bool? result = await Dialog.ShowMessageBox(
            "Confirm Logout",
            "Are you sure you want to logout?",
            yesText: "Logout",
            noText: "Cancel",
            options: new DialogOptions() { MaxWidth = MaxWidth.ExtraSmall, FullWidth = true }
        );

        if (result == true)
        {
            Navigation.NavigateTo("/", true); // perform logout
        }
    }
}